// $env/dynamic/public shim — SvelteKit exposes PUBLIC_* vars here. None are
// needed for the editor view; the GraphQL client that reads them is stubbed.
export const env: Record<string, string> = {};
